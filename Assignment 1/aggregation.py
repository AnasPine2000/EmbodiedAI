class Aggregation(object):
    """docstring for Aggregation."""

    def __init__(self, arg):
        #Initialize the aggregation swarm as a subclass of swarm, so it inherits all the methods fromthe Swarm class.
        super(Aggregation, self).__init__()
        self.arg = arg

    def initialize():
        #This function should initialize the environment. You should create two aggregation areasas well as the containing object.
        # Furthermore similarly to the Flocking implementation, youshould add the cockroach agents to the environment.
        pass
